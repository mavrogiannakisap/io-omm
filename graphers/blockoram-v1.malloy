source: runs is table('duckdb:data/all.csv') {
    query: cleaned is {
        project: test, ssd, s, n, vl, ins, sea, del
    }

    query: ins_by_all is {
        group_by: test, ssd, s, n, vl
        aggregate: ins
    }

    query: sea_by_all is {
        group_by: test, ssd, s, n, vl
        aggregate: sea
    }

    query: del_by_all is {
        group_by: test, ssd, s, n, vl
        aggregate: del
    }
}